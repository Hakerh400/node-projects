'use strict';

const fs = require('fs');
const path = require('path');
const O = require('../omikron');
const esolangs = require('../../esolangs');

const TEST_NAME = 'meadow';

require(`./${TEST_NAME}`);