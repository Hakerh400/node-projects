'use strict';

const fs = require('fs');
const path = require('path');
const assert = require('assert');
const O = require('../omikron');
const bubblegum = require('.');

const cwd = __dirname;
const testDir = path.join(cwd, 'test');
const fin = path.join(test, 'input.txt');
const fout = path.join(test, 'output.txt');

const main = () => {
  const
  bubblegum.encode()
};

main();