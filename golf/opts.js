'use strict';

const fs = require('fs');
const path = require('path');
const assert = require('assert');
const O = require('../omikron');

const MAIN_FUNC_NAME = 'main';

module.exports = {
  MAIN_FUNC_NAME,
};