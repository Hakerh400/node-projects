'use strict';

const fs = require('fs');
const path = require('path');
const assert = require('assert');
const O = require('../../omikron');

const tabs = {
  async create(info){
    
  },

  async update(info){
    
  },

  async move(info){
    
  },

  async replace(info){
    
  },

  async remove(info){
    
  },
};

module.exports = tabs;